# Historical archives

Files in this directory predate the current archive schema (they lack the
embedded experiment descriptor, configHash, and submitted-circuit record)
and were produced by superseded circuit forms such as the deep cat-state
preparation. They are retained as historical evidence of the development
hardware runs. They fail the current verifier's circuit checks by design:
attaching a present-day descriptor to them would misdescribe what was
actually executed.
